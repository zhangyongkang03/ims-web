# IMS 全分析体系 — 前端对接文档

> 更新日期：2026-05-10  
> 后端版本：v3.1（配液 RAMP 抑制 + 报警分级防抖）  
> 适用范围：全部分析模块的前端适配

---

## 📌 总览：前端需要对接的内容

| # | 模块 | 对接方式 | 前端工作量 | 说明 |
|---|------|---------|-----------|------|
| 1 | **秒级风险预测** | WebSocket `fast_risk` | 🔄 需适配 | 新增 `riskLevel="RAMP"` 状态 |
| 2 | **异常报警** | WebSocket `alarm` | 🔄 需适配 | 报警分级（INFO/WARNING/CRITICAL），仅 WARNING+ 推送 |
| 3 | **10s 综合态势分析** | WebSocket `ai_comprehensive_analysis` | 🆕 新增 | 新消息类型，需新增面板 |
| 4 | **批次完成自动分析** | HTTP API（自动生成，被动查询） | 🔄 调整 | 报告现在会自动生成，查询接口不变 |
| 5 | 生产报告 | HTTP API | ✅ 已有 | 不需要改动 |
| 6 | 设备报告 | HTTP API | ✅ 已有 | 不需要改动 |
| 7 | 全流程溯源 | HTTP API | ✅ 已有 | 不需要改动 |

**前端核心改动：#1、#2、#3、#4 四处。**

---

## 〇、配液工艺阶段说明（重要背景）

配液过程有两个阶段，前端展示逻辑完全不同：

```
配液开始（调用 startMixing API）
│
▼  0 ~ 60s: RAMP 期（加料/升温/调浓度）
│  → 值从初始状态逐渐逼近目标值，这是正常行为
│  → ❌ 不报警
│  → ❌ 不做风险评分
│  → ✅ 推送实时值（riskLevel="RAMP"），前端显示调配进度
│
▼  60s 之后: 稳态保持期
│  → 值应稳定在目标范围内
│  → ✅ 正常报警（分级：INFO/WARNING/CRITICAL）
│  → ✅ 正常风险评分（riskLevel="LOW/MEDIUM/HIGH"）
│  → 如果值仍未达标 → 说明设备异常 → 自动报警
│
▼  配液完成（调用 completeMixing API）
│  → 进入罐装阶段
```

> **60 秒** 是 RAMP 时长配置，后端常量控制。

---

## 一、秒级风险预测 — WebSocket `fast_risk` 🔄 需适配

### 1.1 变更说明

**改造前**：所有传感器从第一秒就输出完整风险评分  
**改造后**：配液传感器（BRIX/PH/TEMP）在 RAMP 期间输出特殊的 `riskLevel="RAMP"` 事件

### 1.2 消息格式

#### RAMP 期间（0~60s）— 调配中

```json
{
  "type": "fast_risk",
  "deviceCode": "DEV00001",
  "batchNo": "MIX-WO202605100001",
  "processType": "BRIX",
  "currentValue": 5.23,
  "target": 10.5,
  "minThreshold": 10.0,
  "maxThreshold": 11.0,
  "riskFast": 0.0,
  "riskLevel": "RAMP",
  "deviation": 0.0,
  "slope": 0.0,
  "volatility": 0.0,
  "etaSeconds": -1,
  "timestamp": 1746873030000
}
```

#### 稳态期（60s+）— 正常监控

```json
{
  "type": "fast_risk",
  "deviceCode": "DEV00001",
  "batchNo": "MIX-WO202605100001",
  "processType": "BRIX",
  "currentValue": 10.48,
  "target": 10.5,
  "minThreshold": 10.0,
  "maxThreshold": 11.0,
  "riskFast": 0.12,
  "riskLevel": "LOW",
  "deviation": 0.04,
  "slope": 0.0023,
  "volatility": 0.0156,
  "etaSeconds": -1,
  "timestamp": 1746873120000
}
```

### 1.3 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `type` | String | 固定 `"fast_risk"` |
| `deviceCode` | String | 传感器设备编码 |
| `batchNo` | String | 当前批次号 |
| `processType` | String | 工序类型（BRIX/PH/TEMP 等） |
| `currentValue` | Double | **当前实时值**（RAMP 和稳态都有） |
| `target` | Double | 工艺目标值 |
| `minThreshold` | Double | 工艺下限 |
| `maxThreshold` | Double | 工艺上限 |
| `riskFast` | Double | 风险评分 0~1（RAMP 期固定为 0） |
| `riskLevel` | String | **`"RAMP"` / `"LOW"` / `"MEDIUM"` / `"HIGH"`** |
| `deviation` | Double | 偏离度 0~1（RAMP 期固定为 0） |
| `slope` | Double | EWMA 趋势斜率（RAMP 期固定为 0） |
| `volatility` | Double | 30s 滚动标准差（RAMP 期固定为 0） |
| `etaSeconds` | Double | 预计触线秒数，-1 表示无限远 |
| `timestamp` | Long | 事件时间戳（毫秒） |

### 1.4 前端处理逻辑 ⭐

```javascript
function handleFastRisk(msg) {
  const device = deviceMap[msg.deviceCode]
  if (!device) return

  // 实时值始终更新（RAMP 和稳态都有）
  device.currentValue = msg.currentValue
  device.target = msg.target
  device.minThreshold = msg.minThreshold
  device.maxThreshold = msg.maxThreshold
  device.timestamp = msg.timestamp

  if (msg.riskLevel === 'RAMP') {
    // ========== RAMP 期：调配中 ==========
    device.status = 'RAMP'
    
    // 计算逼近进度（当前值 / 目标值 的比例）
    device.progress = Math.min(100, (msg.currentValue / msg.target * 100)).toFixed(1)
    
    // 不显示风险仪表盘、趋势、ETA
    device.riskFast = null
    device.riskLevel = null
    device.eta = null
    
  } else {
    // ========== 稳态期：正常监控 ==========
    device.status = 'MONITORING'
    device.progress = null
    
    // 显示完整风险信息
    device.riskFast = msg.riskFast
    device.riskLevel = msg.riskLevel  // LOW / MEDIUM / HIGH
    device.slope = msg.slope
    device.volatility = msg.volatility
    device.eta = msg.etaSeconds
    device.deviation = msg.deviation
  }
  
  // 触发 UI 更新
  updateDevicePanel(device)
}
```

### 1.5 UI 建议：配液传感器卡片

```
RAMP 期间 (riskLevel="RAMP"):
┌────────────────────────────────┐
│  🧪 糖度传感器 DEV00001        │
│  状态: 调配中 🔄               │
│                                │
│  当前值  5.23 °Bx              │
│  目标值  10.5 °Bx              │
│  ████████░░░░░░░░  49.8%       │  ← 进度条
│                                │
│  [10.0 ─────────── 11.0]      │  ← 目标范围参考
└────────────────────────────────┘

稳态期间 (riskLevel="LOW/MEDIUM/HIGH"):
┌────────────────────────────────┐
│  🧪 糖度传感器 DEV00001        │
│  状态: 监控中 ✅               │
│                                │
│  当前值  10.48 °Bx             │
│  风险分  ████░░░░  0.12  LOW   │  ← 风险仪表盘
│                                │
│  趋势  → +0.0023/s  稳定      │
│  ETA   无限远                  │
│  [10.0 ──◆──── 11.0]          │  ← 当前值在范围内的位置
└────────────────────────────────┘
```

### 1.6 推送频率

| 传感器类型 | RAMP 期 | 稳态期 |
|-----------|---------|--------|
| 配液传感器（BRIX/PH/TEMP） | 每 2 秒（跟模拟器采样频率一致） | 每 2 秒 |
| 罐装传感器（VOLUME/WEIGHT 等） | 不推送（PER_BOTTLE 类型无趋势） | 不推送 |

---

## 二、异常报警 — WebSocket `alarm` 🔄 需适配

### 2.1 变更说明

**改造前**：任何超标都是 `CRITICAL`，全部推送前端  
**改造后**：三级分级 + 防抖，仅 WARNING/CRITICAL 推送前端

### 2.2 报警分级规则

| 级别 | 触发条件 | 写入 MySQL | 推送前端 | 计入不良品 |
|------|---------|-----------|---------|-----------|
| **INFO** | 偶尔单点超标（连续 <5 次） | ✅ | ❌ 不推送 | ❌ |
| **WARNING** | 连续 ≥5 次超标 | ✅ | ✅ 推送 | ✅（仅罐装传感器） |
| **CRITICAL** | 偏差 ≥200% **或** 连续 ≥15 次超标 | ✅ | ✅ 推送 | ✅（仅罐装传感器） |

> **防抖机制**：值回到正常范围 → 连续超标计数归零。

### 2.3 RAMP 抑制

| 传感器类型 | RAMP 期间（前 60s） | 稳态期 |
|-----------|-------------------|--------|
| 配液传感器（BRIX/PH/TEMP） | ❌ 完全静默，不产生任何级别的报警 | ✅ 正常分级报警 |
| 罐装传感器（VOLUME/WEIGHT 等） | ✅ 正常报警（无 RAMP 概念） | ✅ 正常报警 |

### 2.4 前端收到的报警消息格式

```json
{
  "type": "alarm",
  "batchNo": "MIX-WO202605100001",
  "deviceCode": "DEV00001",
  "processType": "BRIX",
  "alarmLevel": "WARNING",
  "currentValue": 9.85,
  "standardRange": "[10.0000-11.0000]",
  "alarmMsg": "【警告】当前值 9.8500 偏离标准范围 [10.0000 - 11.0000]，已连续超标 5 次",
  "createTime": "2026-05-10 14:31:25"
}
```

**注意：`alarmLevel` 现在有三种值，但前端只会收到 WARNING 和 CRITICAL（INFO 不推送）。**

### 2.5 报警消息中 alarmMsg 示例

| alarmLevel | alarmMsg 示例 |
|------------|--------------|
| WARNING | `【警告】当前值 9.8500 偏离标准范围 [10.0000 - 11.0000]，已连续超标 5 次` |
| CRITICAL（偏差大） | `【严重】当前值 15.2000 严重偏离标准范围 [10.0000 - 11.0000]，偏差达 840%` |
| CRITICAL（持续超标） | `【严重】当前值 9.7200 持续偏离标准范围 [10.0000 - 11.0000]，已连续超标 15 次，建议检查设备` |

### 2.6 前端处理逻辑

```javascript
function handleAlarm(msg) {
  // 前端只会收到 WARNING 和 CRITICAL
  
  const alarm = {
    deviceCode: msg.deviceCode,
    processType: msg.processType,
    level: msg.alarmLevel,       // "WARNING" 或 "CRITICAL"
    value: msg.currentValue,
    range: msg.standardRange,
    message: msg.alarmMsg,
    time: msg.createTime
  }
  
  // 追加到报警列表
  alarmList.unshift(alarm)
  
  // 根据级别不同展示方式
  if (msg.alarmLevel === 'CRITICAL') {
    // 严重报警：红色弹窗 + 声音提醒
    showCriticalAlert(alarm)
    playAlarmSound()
  } else {
    // 警告：黄色提示条（不弹窗，不干扰操作）
    showWarningToast(alarm)
  }
}
```

### 2.7 UI 建议：报警列表颜色

```
┌──────────────────────────────────────────────────────────────┐
│  ⚠️ 实时报警                                                │
├──────────────────────────────────────────────────────────────┤
│  🔴 14:31:40  DEV00001 [BRIX]  CRITICAL                     │
│     【严重】当前值 15.2000 严重偏离，偏差达 840%               │
│                                                              │
│  🟡 14:31:25  DEV00001 [BRIX]  WARNING                      │
│     【警告】当前值 9.8500 偏离，已连续超标 5 次                │
│                                                              │
│  🟡 14:30:58  DEV00003 [Volume]  WARNING                    │
│     【警告】当前值 493.2000 偏离，已连续超标 7 次              │
└──────────────────────────────────────────────────────────────┘
```

> INFO 级别的报警前端不会收到（仅存 MySQL，供事后维保分析查询）。

---

## 三、10s 综合态势分析 — WebSocket 新消息类型 🆕

### 3.1 变更说明

**改造前**：每 10 秒推送 3 条 `ai_analysis` 消息（BRIX/PH/TEMP 各一条）  
**改造后**：每 10~20 秒推送 **1 条** `ai_comprehensive_analysis` 消息（三个参数打包综合分析）

> ⚠️ 原来的 `ai_analysis` 消息**不再推送**给配液车间传感器。  
> 罐装车间传感器也不再推送 `ai_analysis`（改为批次完成后统一诊断，见第四部分）。

### 3.2 WebSocket 连接（不变）

```
ws://localhost:8080/ws/alarm
```

### 3.3 新消息格式：`ai_comprehensive_analysis`

```json
{
  "type": "ai_comprehensive_analysis",
  "deviceCode": "MIXING_WORKSHOP",
  "batchNo": "MIX-WO202605100001",
  "processType": "MIXING_COMPREHENSIVE",
  "riskScore": 0.35,
  "riskLevel": "低风险 🟢",
  "reason": "糖度(10.52Brix)、pH(3.85)、温度(136.8℃)均在正常范围中心附近，标准差小，趋势平稳，三参数间无关联异常。",
  "suggestion": "配液工艺运行正常，建议按常规频率巡检。",
  "finalDecision": "配液工艺运行正常",
  "statsSnapshot": "DEV00001: mean=10.5200 | DEV00002: mean=136.8000 | DEV00003: mean=3.8500 [COMPREHENSIVE]",
  "analysisTime": "2026-05-10T14:30:15",
  "sensorCount": 3
}
```

### 3.4 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `type` | String | 固定 `"ai_comprehensive_analysis"`，用于前端区分消息类型 |
| `deviceCode` | String | 固定 `"MIXING_WORKSHOP"`（代表整个配液车间，非单个传感器） |
| `batchNo` | String | 当前配液批次号（格式 `MIX-工单号`） |
| `processType` | String | 固定 `"MIXING_COMPREHENSIVE"` |
| `riskScore` | Double | 综合风险评分 0~1 |
| `riskLevel` | String | `"低风险 🟢"` / `"中风险 🟡"` / `"高风险 🔴"` |
| `reason` | String | AI 综合分析原因（会涉及多个参数的交叉分析） |
| `suggestion` | String | AI 综合操作建议 |
| `finalDecision` | String | 最终决策 |
| `statsSnapshot` | String | 各传感器均值快照，格式 `设备编码: mean=值 \| ...` |
| `analysisTime` | String | ISO 格式分析时间 |
| `sensorCount` | Integer | 参与分析的传感器数量（2~3） |

### 3.5 前端处理逻辑

```javascript
// 🆕 综合态势分析处理
function handleComprehensiveAnalysis(msg) {
  // 1. 更新配液车间综合面板
  updateMixingWorkshopPanel({
    riskScore: msg.riskScore,
    riskLevel: msg.riskLevel,
    reason: msg.reason,
    suggestion: msg.suggestion,
    sensorCount: msg.sensorCount,
    analysisTime: msg.analysisTime
  })
  
  // 2. 解析各传感器均值（从 statsSnapshot）
  const sensorValues = parseStatsSnapshot(msg.statsSnapshot)
  // sensorValues = [{deviceCode: "DEV00001", mean: 10.52}, ...]
  
  // 3. 风险>0.6 时可做视觉提醒
  if (msg.riskScore >= 0.6) {
    showWarningNotification('配液车间综合预警', msg.reason)
  }
}

// 解析 statsSnapshot 字符串
function parseStatsSnapshot(snapshot) {
  if (!snapshot) return []
  return snapshot.replace(' [COMPREHENSIVE]', '')
    .split(' | ')
    .map(part => {
      const [deviceCode, meanPart] = part.split(': mean=')
      return { deviceCode, mean: parseFloat(meanPart) }
    })
    .filter(item => !isNaN(item.mean))
}
```

### 3.6 UI 建议：配液车间综合态势面板

```
┌─────────────────────────────────────────────────────┐
│  🧪 配液车间综合态势         更新: 14:30:15            │
├─────────────────────────────────────────────────────┤
│                                                     │
│  综合风险评分    ████████░░  0.35  低风险 🟢           │
│                                                     │
│  ┌─── 三参数实时概况 ──────────────────────┐          │
│  │  🟢 糖度(Brix)   10.52   [10.0~11.0]  │          │
│  │  🟢 温度(℃)      136.8   [135~140]    │          │
│  │  🟢 pH            3.85   [3.5~4.2]    │          │
│  └─────────────────────────────────────────┘          │
│                                                     │
│  🤖 AI 综合分析:                                     │
│  糖度/pH/温度三参数均在正常范围中心附近，趋势平稳...       │
│                                                     │
│  💡 建议: 配液工艺运行正常，按常规频率巡检。              │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 3.7 消息频率

| 条件 | 推送频率 |
|------|---------|
| 3 个 PROCESS 传感器都在线 | 约 10~15 秒/次（缓冲凑齐后推送） |
| 只有 2 个在线 | 约 10~20 秒/次 |
| 不足 2 个在线 | 不推送 |
| 批次未运行(batch_status≠1) | 不推送 |

---

## 四、批次完成自动分析 — 行为变更 🔄

### 4.1 变更说明

**改造前**：批次完成后什么都不做，前端需要手动点击「生成报告」按钮  
**改造后**：批次完成后后端 **自动异步** 生成以下内容，前端只需 **被动查询**

| 自动生成内容 | 存储位置 | 查询方式 |
|-------------|---------|---------|
| 批次质量报告 | Redis 缓存(12h) | `GET /batch-quality/report/{batchNo}` |
| 配液车间综合诊断 | MySQL `mes_ai_analysis_record` | `GET /ai-decision/history` |
| 罐装车间设备精度诊断 | MySQL `mes_ai_analysis_record` | `GET /ai-decision/history` |

### 4.2 前端改动点

#### 4.2.1 批次质量报告的调用时机调整

```javascript
// ❌ 改造前：手动点按钮才调接口
// button onClick -> fetchBatchQualityReport(batchNo)

// ✅ 改造后：批次完成后自动生成，前端可直接查询
// 方式1: 生产报告中点击「批次明细」的某一行，钻取到质量报告
// 方式2: 溯源页面中自动关联

// API 接口没变，还是同一个
export function getBatchQualityReport(batchNo) {
  return request.get(`/batch-quality/report/${batchNo}`)
}

// 时序数据(折线图)
export function getTimeSeriesData(batchNo, deviceCode, limit = 5000) {
  return request.get(`/batch-quality/timeseries/${batchNo}/${deviceCode}`, {
    params: { limit }
  })
}
```

#### 4.2.2 批次质量报告响应格式（不变）

```json
{
  "code": 1,
  "data": {
    "batchNo": "B20260510001",
    "orderNo": "WO202605100001",
    "productName": "芒果汁 500ml",
    "targetQty": 1000,
    "actualQty": 1000,
    "badQty": 12,
    "yieldRate": 98.80,
    "batchStatus": "已完成",
    "startTime": "2026-05-10T08:00:00",
    "endTime": "2026-05-10T09:45:00",
    "durationMinutes": 105,
    
    "overallCpk": 1.45,
    "cpkPassCount": 4,
    "cpkFailCount": 1,
    "bestStationCode": "DEV00005",
    "worstStationCode": "DEV00008",
    
    "processQualityList": [
      {
        "processName": "灌装",
        "processType": "Volume",
        "equipName": "灌装机A",
        "cpk": 1.62,
        "cpkLevel": "优秀",
        "outOfRangeRate": 0.3,
        "alarmCount": 2,
        "qualityImpact": "灌装精度优异，对产品质量无负面影响"
      }
    ],
    
    "equipmentStatsList": [
      {
        "equipCode": "EQ001",
        "equipName": "灌装机A",
        "processType": "FILLING",
        "cpk": 1.62,
        "cpkLevel": "优秀",
        "alarmCount": 2,
        "outOfRangeRate": 0.3,
        "healthComment": "设备精度良好",
        "sensorStatsList": [
          {
            "deviceCode": "DEV00005",
            "deviceName": "灌装容量传感器",
            "parameterName": "灌装量",
            "unit": "ml",
            "targetValue": 500.0,
            "minThreshold": 495.0,
            "maxThreshold": 505.0,
            "mean": 500.12,
            "stdDev": 0.85,
            "maxVal": 503.2,
            "minVal": 497.1,
            "sampleCount": 1000,
            "outOfRangeCount": 3,
            "outOfRangeRate": 0.30,
            "cpk": 1.62,
            "cpkLevel": "优秀"
          }
        ]
      }
    ],
    
    "totalAlarmCount": 12,
    "alarmDistribution": [
      { "deviceCode": "DEV00008", "processType": "Torque", "alarmCount": 8, "alarmLevel": "WARN" },
      { "deviceCode": "DEV00005", "processType": "Volume", "alarmCount": 2, "alarmLevel": "WARN" }
    ],
    
    "qualityGrade": "B",
    "qualityScore": 82.5,
    "aiReportSummary": "本批次综合良率98.80%，整体质量良好...",
    "aiRootCauseAnalysis": "不良品主要来源于封盖工序...",
    "aiImprovementSuggestion": "建议本批次结束后校准封盖头扭矩参数...",
    "aiRiskHighlights": "1. 封盖扭矩CPK<1.0，需紧急关注\n2. 灌装精度优秀，可降低巡检频率",
    "reportTime": "2026-05-10T09:46:00"
  }
}
```

#### 4.2.3 生产报告 → 批次钻取联动

```javascript
function onBatchRowClick(row) {
  const batchNo = row.batchNo
  router.push(`/batch-quality/${batchNo}`)
}

async function loadBatchQualityReport(batchNo) {
  const { data } = await getBatchQualityReport(batchNo)
  if (!data) {
    showMessage('报告正在生成中，请稍后刷新')
    return
  }
  // 渲染报告页面...
}
```

> **注意**：批次完成后报告是异步生成的，一般需要 10~30 秒。建议加 loading + 自动重试。

### 4.3 设备精度诊断结果查询

```javascript
export function getAiAnalysisHistory(params) {
  return request.get('/ai-decision/history', {
    params: {
      pageNum: params.pageNum || 1,
      pageSize: params.pageSize || 20,
      deviceCode: params.deviceCode,
      riskLevel: params.riskLevel
    }
  })
}

export function getLatestAiAnalysis(deviceCode) {
  return request.get(`/ai-decision/latest/${deviceCode}`)
}
```

---

## 五、WebSocket 完整消息路由

前端只需连接 **一个** WebSocket 地址：`ws://localhost:8080/ws/alarm`

```javascript
const ws = new WebSocket('ws://localhost:8080/ws/alarm')

ws.onmessage = (event) => {
  const msg = JSON.parse(event.data)
  
  switch (msg.type) {
    case 'fast_risk':
      // 秒级风险预测（含 RAMP 模式）
      handleFastRisk(msg)
      break
      
    case 'alarm':
      // 异常报警（仅 WARNING + CRITICAL）
      handleAlarm(msg)
      break
      
    case 'ai_comprehensive_analysis':
      // 配液车间综合态势（10~20s 一次）
      handleComprehensiveAnalysis(msg)
      break
      
    case 'dashboard_data':
      // 产量/良率实时数据
      handleDashboardData(msg)
      break
      
    case 'ai_analysis':
      // ⚠️ 已废弃，不再推送，保留兼容代码即可
      break
  }
}
```

### WebSocket 消息类型汇总

| type | 频率 | 说明 | 前端动作 |
|------|------|------|---------|
| `fast_risk` | 每 2s/每配液设备 | 秒级预测 + RAMP 实时值 | 按 `riskLevel` 区分显示模式 |
| `alarm` | 有 WARNING/CRITICAL 才推 | 分级报警（INFO 不推送） | 按级别用不同颜色/方式展示 |
| `ai_comprehensive_analysis` | 10~20s/批次 | 配液综合态势 | 更新综合分析面板 |
| `dashboard_data` | 批次运行时 | 产量/良率 | 更新看板 |
| `ai_analysis` | ~~已废弃~~ | 不再推送 | 保留兼容代码即可 |

---

## 六、配液阶段完整时间线 & 前端展示

```
时间轴:  0s ────── 60s ───────────────────────── 完成
         │         │                              │
         ▼         ▼                              ▼
fast_risk:  RAMP模式      LOW/MEDIUM/HIGH 正常监控
            显示调配进度    显示风险仪表盘+趋势
                    
alarm:      完全静默       INFO(不推) / WARNING(推) / CRITICAL(推)
                    
综合分析:   不推送          每10~20s推送 ai_comprehensive_analysis
            (数据积累不足)   (AI综合三参数分析)
```

### 前端看板建议布局

```
┌────────────────────────────────────────────────────────────────┐
│  🏭 配液车间实时监控                                           │
├──────────────┬──────────────┬──────────────┬───────────────────┤
│ 🧪 糖度      │ 🌡️ 温度      │ 🧫 pH       │ 🤖 AI 综合态势  │
│              │              │              │                   │
│  [RAMP 期]   │  [RAMP 期]   │  [RAMP 期]   │  ────────         │
│  5.23 °Bx    │  45.2 ℃     │  5.12        │  数据积累中...    │
│  ████░░ 50%  │  ███░░░ 33%  │  ██░░░░ 27%  │                   │
│              │              │              │                   │
│  [稳态期]    │  [稳态期]    │  [稳态期]    │  综合风险 0.35    │
│  10.48 °Bx   │  136.8 ℃    │  3.85        │  低风险 🟢        │
│  风险 0.12   │  风险 0.08   │  风险 0.15   │  AI: 三参数均正常  │
│  LOW 🟢      │  LOW 🟢      │  LOW 🟢      │  建议: 按常规巡检  │
├──────────────┴──────────────┴──────────────┴───────────────────┤
│  ⚠️ 实时报警  (仅显示 WARNING + CRITICAL)                       │
│  🟡 14:31:25  DEV00001 [BRIX] WARNING  连续超标5次             │
│  🔴 14:31:40  DEV00001 [BRIX] CRITICAL 偏差达840%             │
└────────────────────────────────────────────────────────────────┘
```

---

## 七、已有模块（无需改动）快速参考

### 7.1 生产报告 — HTTP API

```javascript
export function getProductionReport(type = 'daily', date) {
  return request.get('/production-report', { params: { type, date } })
}
```

### 7.2 设备报告 — HTTP API

```javascript
export function getDeviceDailyReport(date) {
  return request.get('/device-report/daily', { params: { date } })
}
export function getDeviceWeeklyReport(date) {
  return request.get('/device-report/weekly', { params: { date } })
}
export function getDeviceMonthlyReport(date) {
  return request.get('/device-report/monthly', { params: { date } })
}
```

### 7.3 全流程溯源 — HTTP API

```javascript
export function getFullTraceability(batchNo) {
  return request.get(`/traceability/${batchNo}/full-chain`)
}
```

---

## 八、API 接口全清单

### 8.1 批次质量报告

| 方法 | URL | 说明 |
|------|-----|------|
| GET | `/batch-quality/report/{batchNo}` | 获取批次质量报告（批次完成后自动生成） |
| GET | `/batch-quality/timeseries/{batchNo}/{deviceCode}?limit=5000` | 指定设备时序数据（折线图） |
| DELETE | `/batch-quality/cache/{batchNo}` | 清除缓存强制重新生成 |

### 8.2 AI 分析记录

| 方法 | URL | 说明 |
|------|-----|------|
| GET | `/ai-decision/latest/{deviceCode}` | 设备最新 AI 分析结果（缓存） |
| GET | `/ai-decision/history?pageNum=1&pageSize=20&deviceCode=&riskLevel=` | AI 分析历史分页 |

### 8.3 生产报告

| 方法 | URL | 说明 |
|------|-----|------|
| GET | `/production-report?type=daily&date=2026-05-09` | 生产日/周/月报 |

### 8.4 设备报告

| 方法 | URL | 说明 |
|------|-----|------|
| GET | `/device-report/daily?date=2026-05-09` | 设备日报 |
| GET | `/device-report/daily/today` | 今日设备日报 |
| GET | `/device-report/daily/latest` | 最新缓存日报 |
| GET | `/device-report/weekly?date=2026-05-09` | 设备周报 |
| GET | `/device-report/monthly?date=2026-05-09` | 设备月报 |

### 8.5 溯源

| 方法 | URL | 说明 |
|------|-----|------|
| GET | `/traceability/{batchNo}/full-chain` | 全流程溯源 |

---

## 九、注意事项

### 9.1 `riskLevel="RAMP"` 是新增的第四种风险等级

原有三种：`LOW` / `MEDIUM` / `HIGH`  
新增一种：`RAMP`（仅配液传感器在前 60 秒出现）

前端**必须处理此情况**，否则 RAMP 期间仪表盘会显示异常。

### 9.2 `ai_analysis` 消息已废弃

原来的 `ai_analysis` WebSocket 消息不再推送。前端替换为 `ai_comprehensive_analysis` 即可。

### 9.3 报警级别 INFO 不推送前端

INFO 级别只写入 MySQL，不通过 WebSocket 推送。前端如需查询历史 INFO 报警，使用：

```javascript
// 查询报警历史（含 INFO）
export function getAlarmHistory(params) {
  return request.get('/alarm/list', { params })
}
```

### 9.4 批次质量报告的延迟

批次完成后异步生成，需 10~30 秒。建议加重试：

```javascript
async function fetchWithRetry(batchNo, maxRetry = 3) {
  for (let i = 0; i < maxRetry; i++) {
    const { data } = await getBatchQualityReport(batchNo)
    if (data) return data
    await new Promise(r => setTimeout(r, 5000))
  }
  return null
}
```

### 9.5 综合分析的 deviceCode

综合态势分析的 `deviceCode` 固定为 `"MIXING_WORKSHOP"`，代表整个配液车间。

### 9.6 日期格式统一

所有 API 的 `date` 参数格式：`yyyy-MM-dd`（如 `2026-05-10`）  
所有时间字段返回格式：`yyyy-MM-ddTHH:mm:ss`（ISO 8601）

### 9.7 不良品计数逻辑

报警推送到后端 Kafka Consumer 后，会自动判断：
- **罐装传感器**（VOLUME/WEIGHT/TORQUE 等）的 WARNING/CRITICAL 报警 → 自动给批次 `bad_qty + 1`
- **配液传感器**（BRIX/PH/TEMP）的报警 → 只记录，不计入不良品

前端无需处理此逻辑，看板上的不良品数量会通过 `dashboard_data` 实时推送。
